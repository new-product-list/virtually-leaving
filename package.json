{
  "name": "virtually-leaving",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "author": "Shaun-Farrell",
  "workspaces": [
    "packages/design-system",
    "packages/web-app",
    "packages/api"
  ],
  "scripts": {
    "mono": "yarn workspaces foreach run mono",
    "sb": "yarn workspace design-system run storybook",
    "web": "yarn workspace web-app run start",
    "api-db": "yarn workspace design-system run start-db",
    "api": "yarn workspace design-system run offline"
  }
}
